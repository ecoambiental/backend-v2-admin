export const INSTITUTIONS_MAP: Record<string, number> = {
  ecoambiental: 1,
  ghamec: 2,
  acm: 3,
  agroambiental: 4,
  geplane: 5,
};

export const ALLOWED_INSTITUTIONS = Object.keys(INSTITUTIONS_MAP);
